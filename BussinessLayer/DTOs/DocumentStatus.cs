namespace BussinessLayer.DTOs
{
    /// <summary>
    /// Trạng thái xử lý của tài liệu (ở tầng nghiệp vụ DTO)
    /// </summary>
    public enum DocumentStatus
    {
        Pending = 0,
        Indexed = 1,
        Failed = 2
    }
}
