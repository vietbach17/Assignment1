@model IEnumerable<BussinessLayer.DTOs.ChapterDto>
@{
    ViewData["Title"] = $"Chương - {ViewBag.SubjectCode}";
    Layout = null;
}
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        brand: {
                            dark: '#1e3b31',
                            active: '#2c5344',
                            button: '#376b56',
                            text: '#222222',
                            gray: '#f4f6f8'
                        }
                    }
                }
            }
        }
    </script>
</head>
<body class="bg-gray-50 flex h-screen overflow-hidden text-gray-800">

    <aside class="w-64 bg-brand-dark text-white flex flex-col">
        <div class="p-6 flex items-center space-x-3">
            <div class="bg-white/20 p-2 rounded-lg">
                <i class="fa-solid fa-book-open text-white"></i>
            </div>
            <div>
                <h1 class="font-bold text-lg leading-tight">StudyMind</h1>
                <p class="text-xs text-gray-300">Quản lý nội dung</p>
            </div>
        </div>

        @if (User.IsInRole("Admin"))
        {
            <div class="px-4 py-2">
                <p class="text-xs font-semibold text-gray-400 mb-3 uppercase tracking-wider">Menu Chính</p>
                <ul class="space-y-1">
                    <li>
                        <a asp-action="Dashboard" asp-controller="Admin" class="flex items-center space-x-3 px-4 py-2.5 rounded-lg text-sm font-medium text-gray-300 hover:bg-brand-active/50 hover:text-white transition">
                            <i class="fa-solid fa-chart-line w-5"></i>
                            <span>Bảng điều khiển</span>
                        </a>
                    </li>
                    <li>
                        <a asp-action="Users" asp-controller="Admin" class="flex items-center space-x-3 px-4 py-2.5 rounded-lg text-sm font-medium text-gray-300 hover:bg-brand-active/50 hover:text-white transition">
                            <i class="fa-solid fa-users w-5"></i>
                            <span>Người Dùng</span>
                        </a>
                    </li>
                    <li>
                        <a asp-action="Index" asp-controller="Home" class="flex items-center space-x-3 px-4 py-2.5 rounded-lg text-sm font-medium text-gray-300 hover:bg-brand-active/50 hover:text-white transition">
                            <i class="fa-solid fa-arrow-left w-5"></i>
                            <span>Về trang chủ</span>
                        </a>
                    </li>
                </ul>
            </div>
        }
        else if (User.IsInRole("Lecturer"))
        {
            <div class="px-4 py-2">
                <p class="text-xs font-semibold text-gray-400 mb-3 uppercase tracking-wider">Menu Chính</p>
                <ul class="space-y-1">
                    <li>
                        <a asp-action="Index" asp-controller="Home" class="flex items-center space-x-3 px-4 py-2.5 rounded-lg text-sm font-medium text-gray-300 hover:bg-brand-active/50 hover:text-white transition">
                            <i class="fa-solid fa-border-all w-5"></i>
                            <span>Tổng quan</span>
                        </a>
                    </li>
                    <li>
                        <a asp-action="Index" asp-controller="Subjects" class="flex items-center space-x-3 px-4 py-2.5 rounded-lg text-sm font-medium text-gray-300 hover:bg-brand-active/50 hover:text-white transition">
                            <i class="fa-solid fa-book w-5"></i>
                            <span>Môn Học</span>
                        </a>
                    </li>
                    <li>
                        <a asp-action="Index" asp-controller="Document" class="flex items-center space-x-3 px-4 py-2.5 rounded-lg text-sm font-medium text-gray-300 hover:bg-brand-active/50 hover:text-white transition">
                            <i class="fa-regular fa-file-lines w-5"></i>
                            <span>Tài liệu</span>
                        </a>
                    </li>
                </ul>
            </div>
        }

        <div class="px-4 py-6 border-t border-white/5 mt-auto">
            <a asp-action="Logout" asp-controller="Auth" class="flex items-center space-x-3 px-4 py-2.5 rounded-lg text-sm font-medium text-gray-300 hover:bg-brand-active/50 hover:text-white transition">
                <i class="fa-solid fa-sign-out-alt w-5"></i>
                <span>Đăng xuất</span>
            </a>
        </div>
    </aside>

    <main class="flex-1 flex flex-col h-screen overflow-hidden">
        <header class="h-16 bg-white flex items-center justify-between px-8 border-b border-gray-100">
            <div class="flex items-center gap-4 text-sm text-gray-500">
                <a asp-controller="Subjects" asp-action="Index" class="hover:text-brand-active transition">Môn học</a>
                <i class="fa-solid fa-chevron-right text-xs"></i>
                <span class="font-semibold text-gray-800">Các chương</span>
            </div>
            
            <div class="flex items-center space-x-5">
                @if (User.Identity?.IsAuthenticated == true)
                {
                    <button class="text-gray-400 hover:text-gray-600 relative">
                        <i class="fa-regular fa-bell"></i>
                        <span class="absolute top-0 right-0 w-2 h-2 bg-red-500 rounded-full"></span>
                    </button>
                    <div class="flex items-center space-x-3 cursor-pointer">
                        <div class="w-9 h-9 bg-teal-100 text-teal-700 rounded-full flex items-center justify-center font-bold text-sm">
                            <i class="fa-solid fa-user"></i>
                        </div>
                        <div>
                            <p class="text-sm font-semibold text-gray-700 leading-tight">@User.Identity.Name</p>
                            <p class="text-xs text-gray-500">@User.Claims.FirstOrDefault(c => c.Type == System.Security.Claims.ClaimTypes.Role)?.Value</p>
                        </div>
                    </div>
                }
            </div>
        </header>

        <div class="flex-1 overflow-y-auto p-8">
            <div class="mb-8 flex justify-between items-end">
                <div>
                    <p class="text-sm text-gray-500 font-semibold tracking-wide uppercase mb-1">Cấu trúc môn học</p>
                    <h2 class="text-2xl font-bold text-gray-800 mb-2">Danh sách Chương: <span class="text-brand-active">@ViewBag.SubjectCode</span></h2>
                    <p class="text-gray-500 text-sm">Xem và quản lý các chương học thuộc môn học này.</p>
                </div>
                @if (User.IsInRole("Admin") || User.IsInRole("Lecturer"))
                {
                    <a asp-action="Create" asp-route-subjectId="@ViewBag.SubjectId" class="bg-brand-button hover:bg-brand-active text-white font-medium py-2 px-4 rounded-xl transition shadow-sm inline-flex items-center gap-2">
                        <i class="fa-solid fa-plus"></i>
                        Thêm chương mới
                    </a>
                }
            </div>

            @if (TempData["SuccessMessage"] != null)
            {
                <div class="mb-6 p-4 bg-green-50 border border-green-200 rounded-lg text-green-700 text-sm flex items-center gap-2">
                    <i class="fa-solid fa-check-circle"></i> @TempData["SuccessMessage"]
                </div>
            }
            @if (TempData["ErrorMessage"] != null)
            {
                <div class="mb-6 p-4 bg-red-50 border border-red-200 rounded-lg text-red-700 text-sm flex items-center gap-2">
                    <i class="fa-solid fa-exclamation-circle"></i> @TempData["ErrorMessage"]
                </div>
            }

            @if (!Model.Any())
            {
                <div class="bg-white p-12 rounded-2xl shadow-sm border border-gray-100 text-center">
                    <i class="fa-solid fa-list-ul text-4xl text-gray-300 mb-4 block"></i>
                    <h3 class="text-lg font-bold text-gray-800 mb-2">Chưa có chương học nào</h3>
                    <p class="text-gray-500 text-sm mb-6">Môn học này hiện chưa được tạo cấu trúc chương.</p>
                    @if (User.IsInRole("Admin") || User.IsInRole("Lecturer"))
                    {
                        <a asp-action="Create" asp-route-subjectId="@ViewBag.SubjectId" class="inline-block bg-brand-button hover:bg-brand-active text-white font-medium py-2 px-6 rounded-xl transition">
                            Tạo chương đầu tiên
                        </a>
                    }
                </div>
            }
            else
            {
                <div class="bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden">
                    <table class="w-full text-left border-collapse">
                        <thead>
                            <tr class="bg-gray-50 text-gray-600 text-sm border-b border-gray-100">
                                <th class="py-4 px-6 font-semibold w-24">Chương</th>
                                <th class="py-4 px-6 font-semibold">Tên chương</th>
                                <th class="py-4 px-6 font-semibold hidden md:table-cell">Mô tả</th>
                                <th class="py-4 px-6 font-semibold text-right">Thao tác</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var chapter in Model)
                            {
                                <tr class="border-b border-gray-50 hover:bg-gray-50 transition">
                                    <td class="py-4 px-6">
                                        <div class="w-10 h-10 bg-brand-gray text-brand-active rounded-lg flex items-center justify-center font-bold">
                                            @chapter.ChapterNumber
                                        </div>
                                    </td>
                                    <td class="py-4 px-6 font-medium text-gray-800">
                                        @chapter.ChapterTitle
                                    </td>
                                    <td class="py-4 px-6 text-sm text-gray-500 hidden md:table-cell">
                                        @if (!string.IsNullOrEmpty(chapter.Description))
                                        {
                                            @(chapter.Description.Length > 80 ? chapter.Description.Substring(0, 80) + "..." : chapter.Description)
                                        }
                                        else
                                        {
                                            <span class="italic opacity-50">Không có mô tả</span>
                                        }
                                    </td>
                                    <td class="py-4 px-6 text-right">
                                        <div class="flex justify-end items-center gap-2">
                                            <a asp-action="Details" asp-route-id="@chapter.Id" class="p-2 text-blue-600 hover:bg-blue-50 rounded-lg transition" title="Chi tiết">
                                                <i class="fa-solid fa-eye"></i>
                                            </a>
                                            @if (User.IsInRole("Admin") || User.IsInRole("Lecturer"))
                                            {
                                                <a asp-action="Edit" asp-route-id="@chapter.Id" class="p-2 text-orange-500 hover:bg-orange-50 rounded-lg transition" title="Sửa">
                                                    <i class="fa-solid fa-pen-to-square"></i>
                                                </a>
                                                <a asp-action="Delete" asp-route-id="@chapter.Id" class="p-2 text-red-600 hover:bg-red-50 rounded-lg transition" title="Xóa">
                                                    <i class="fa-solid fa-trash"></i>
                                                </a>
                                            }
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
        </div>
    </main>

</body>
</html>
